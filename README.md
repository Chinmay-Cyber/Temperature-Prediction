# Temperature-Prediction
This project builds a machine learning model to predict temperature using historical climate data. The dataset is cleaned, reshaped using melt, and categorical features like Area and Months are encoded. A Random Forest Regressor is trained on Area, Month, and Year to predict temperature and evaluated using Mean Absolute Error.
